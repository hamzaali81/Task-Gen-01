meta {
  name: Get Profile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/auth/profile
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Returns the authenticated user's profile and workspace memberships.
}
