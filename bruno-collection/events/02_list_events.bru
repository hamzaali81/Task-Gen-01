meta {
  name: List Events
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/events
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-workspace-id: {{workspaceId}}
}

docs {
  Lists all events in the workspace with their total budget computed.
}
